{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean:build": "node -e \"const fs=require('fs');fs.rmSync('.next',{recursive:true,force:true})\"",
    "clean:dev": "node -e \"const fs=require('fs');fs.rmSync('.next-dev',{recursive:true,force:true})\"",
    "dev": "set NEXT_DIST_DIR=.next-dev&& next dev -p 3000",
    "dev:clean": "npm run clean:dev && set NEXT_DIST_DIR=.next-dev&& next dev -p 3000",
    "build": "npm run clean:build && next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "axios": "^1.16.1",
    "next": "14.2.35",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-hook-form": "^7.75.0",
    "zustand": "^5.0.13"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.35",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
